import React from "react";
import { Checkbox, TableCell } from "@material-ui/core";

// habit check box component take habit name and habit id
const HabitCheckBox = () => {
  // need habit status state

  // for index use moment day week

  // declare handle change method

  return (
    // return for each day of the week

    // also provide associate lable for check box use "for" tag
    <TableCell align="center">
      <Checkbox></Checkbox>
    </TableCell>
  );
};

export default HabitCheckBox;
